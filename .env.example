# Railway Environment Variables Template
# Copy this file to .env for local development

# Application Configuration
NODE_ENV=production
PORT=8000

# CORS Configuration (for Railway deployment)
FRONTEND_URL=https://your-app-name.railway.app

# Search Configuration (uses public SearxNG instance by default)
SEARXNG_BASE_URL="https://search.ononoki.org"

# Backend API Keys (for enhanced agent capabilities)
# These are stored securely on the server, not exposed to users
# Copy this file to .env and add your actual keys

# Claude API Key (Required for enhanced Code Agent)
CLAUDE_API_KEY=sk-ant-api03-your_actual_key_here

# Note: User API keys are still provided at runtime through the UI
# The backend keys above are for enhanced features only

# Database Configuration (if you add database later)
# DATABASE_URL=postgresql://username:password@host:port/database

# Optional: For local development
# NODE_ENV=development
# FRONTEND_URL=http://localhost:3000